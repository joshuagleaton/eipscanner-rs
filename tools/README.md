# Tools

| Tool | Description |
|---|---|
| `opener-tests.sh` | Starts the OpENer sample adapter in Docker and runs the integration tests in `tests/opener.rs` against it. Run `tools/opener-tests.sh -h`. |
